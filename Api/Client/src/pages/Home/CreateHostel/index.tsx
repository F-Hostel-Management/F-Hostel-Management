import * as React from 'react';

interface ICreateHostelProps {
}

const CreateHostel: React.FunctionComponent<ICreateHostelProps> = (props) => {
  return (
    <h1>Create Hostel</h1>
  );
};

export default CreateHostel;
