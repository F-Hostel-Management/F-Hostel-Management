import * as React from 'react';

interface ISidebarIconOnlyProps {
}

const SidebarIconOnly: React.FunctionComponent<ISidebarIconOnlyProps> = (props) => {
  return (
      <div>
          <p>Sidebar Icon Only</p>
      </div>
  );
};

export default SidebarIconOnly;
