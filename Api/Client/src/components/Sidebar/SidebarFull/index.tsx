import * as React from 'react';

interface ISidebarFullProps {
}

const SidebarFull: React.FunctionComponent<ISidebarFullProps> = (props) => {
  return (
      <div>
          <p>Sidebar Full</p>
      </div>
  );
};

export default SidebarFull;
