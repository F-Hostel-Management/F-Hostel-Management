import * as React from 'react';

interface IHeaderHomeProps {
}

const HeaderHome: React.FunctionComponent<IHeaderHomeProps> = (props) => {
  return (
      <nav>
          <p>Header Home</p>
      </nav>
  );
};

export default HeaderHome;
