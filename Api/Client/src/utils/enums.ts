export enum ERole {
    TENANT_ROLE = 0,
    MANAGER_ROLE = 1,
    OWNER_ROLE = 2,
}

export enum EGender {
    'Male' = 0,
    'Female' = 1,
    'Unknown' = 2,
}

export enum ECommitmentStatus {
    Expired = 'Expired',
    Approved = 'Approved',
    Pending = 'Pending',
    Active = 'Active',
}

export enum ERoomType {
    'Single',
    'Double',
}

export enum ERoomStatus {
    'Finished',
    'Renting',
}
