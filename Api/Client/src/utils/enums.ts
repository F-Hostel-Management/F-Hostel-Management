export enum IRole {
    TENANT_ROLE = 0,
    MANAGER_ROLE = 1,
    OWNER_ROLE = 2,
}

export enum IGender {
    'Male',
    'Female',
    'Unknown',
}

export enum ECommitmentStatus {
    'Closed',
    'Pending',
    'Active',
}

export enum ERoomType {
    'Single',
    'Double',
}

export enum ERoomStatus {
    'Finished',
    'Renting',
}
